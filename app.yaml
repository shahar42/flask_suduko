runtime: custom
env: flex
runtime_config:
  python_version: 3.11

entrypoint: waitress-serve --host=0.0.0.0 --port=$PORT app:app  # Use Waitress instead of Gunicorn

instance_class: F2

env_variables:
  FLASK_ENV: "production"
  SECRET_KEY: "your-secret-key"
